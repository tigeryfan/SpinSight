[project]
name = "spindsight"
version = "0.1.0"
description = "Greenwald laundry scraper + Telegram notifier"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tabulate>=0.9",
]

[tool.uv]
package = false